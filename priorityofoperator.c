#include<stdio.h>

int main()
{
	int x = 5, y = 6, z = 7, result;
	result = x + y * z;
	printf("\n the expression is  x + y * z \n");
	printf("\n x = %d, y = %d, z = %d\n", x, y, z);
	printf("\n the result of the expression is %d + %d * %d = %d\n", x, y, z, result);

	return 0;
}
